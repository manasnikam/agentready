# Screen recordings

Drop captures here as `<scene-id>.mp4` (hook, benchmark, copilot, store,
close), then flip the matching flag in `../../src/scenes.ts` `RECORDINGS`.
The shot list is in `../../script.md`.
